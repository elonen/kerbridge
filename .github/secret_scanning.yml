# The token corpus matches GitHub's Entra access-token pattern by construction,
# so push protection blocks it. The tokens are synthetic and signed by a
# throwaway key that make_fixtures.py regenerates and never commits --
# see testbench/fixtures/entra-token/.gitignore.
paths-ignore:
  - "testbench/fixtures/entra-token/*.jwt"
