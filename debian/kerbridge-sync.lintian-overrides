# The binary is a statically linked musl artifact on purpose: one package must
# install unchanged on every supported release, which a libc6-linked binary
# cannot do, and there is no Debian-packaged musl runtime to depend on instead.
#
# One line, unqualified, because every architecture is built static-PIE --
# ET_DYN with a PT_DYNAMIC and no NEEDED entries. lintian's Binaries::Static
# skips its `statically-linked-binary` tag for anything whose file type reads as
# a shared object or pie executable, so Binaries::Prerequisites reports a shared
# library missing its dependencies instead, on every architecture alike.
#
# A build that produced a plain ET_EXEC would fire `statically-linked-binary`
# and leave this override unused. debian/Dockerfile asserts static-pie for every
# binary, so that fails there first.
#
# Measured on lintian 2.116.3 and 2.122.0, identically. Re-check with `file` on
# the built binary and `lintian --show-overrides <deb>`: the tag shows as an
# `O:` line.
kerbridge-sync: shared-library-lacks-prerequisites [usr/sbin/kerbridge-sync]
